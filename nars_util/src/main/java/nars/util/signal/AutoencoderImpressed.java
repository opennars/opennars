package nars.util.signal;

/**
 * stores a queue of historic inputs (episodes) which caused significant changes
 * in learning (had a high degree of error). these can then be replayed to
 * destabilize the memory out of attractors which it may get stuck in
 */
public class AutoencoderImpressed {

	// TODO

}
