package nars.task.filter;

/**
 * TODO
 */
class SentenceEquivalentToParentTasks {

	/*
	 * if (Global.DEBUG) { //Check EXACT equivalence, slightly different because
	 * it includes creationTime test: if
	 * (task.sentence.equivalentTo(task.getParentTask().sentence, true, true,
	 * true, true, true)) { memory.emit(Events.ERR.class,
	 * "deriveTask: new task's equal to parentTask " + task); //return false; }
	 * }
	 */

}
