package nars.bag;

/**
 * tests the 'Level' inner class of LevelBag
 */
public class LevelBagLevelTest {

	// @Test
	// public void testLevel1() {
	// LevelBag.Level l = new LevelBag.Level(10);
	// l.print();
	//
	// l.add(new BagPerf.NullItem(0.5f, "a"));
	// l.print();
	//
	// l.add("b");
	// l.print();
	//
	// assertEquals(2, l.size());
	// assertTrue(l.contains("a"));
	//
	// l.add("c");
	// l.print();
	//
	// assertEquals(3, l.size());
	//
	// assertEquals("a", l.getFirst());
	// assertEquals("c", l.getLast());
	//
	// assertEquals("a", l.removeFirst());
	// assertEquals(2, l.size());
	//
	// assertEquals("b", l.getFirst());
	//
	// l.add("d");
	// assertEquals(3, l.size());
	// assertEquals("d", l.getLast());
	// assertEquals("b", l.getFirst());
	// l.print();
	//
	// assertEquals("c", l.remove("c"));
	// l.print();
	//
	// assertEquals(2, l.size());
	// l.removeFirst();
	// l.print();
	//
	// assertEquals(1, l.size());
	// l.removeFirst();
	// l.print();
	//
	// assertEquals(0, l.size());
	//
	// }
}
