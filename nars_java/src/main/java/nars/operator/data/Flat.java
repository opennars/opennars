package nars.operator.data;

/**
 * pushes a term tree to a type=product|set, in order=breadth|depth
 */
public class Flat {

    public Flat(boolean productOrSet, boolean breadthOrDepth) {
        //generate each of the 4 different operator names
    }
}
