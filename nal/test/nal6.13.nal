'********** multiple variable elimination

'Every lock can be opened by every key.
<(&&,<$x --> key>,<$y --> lock>) ==> <$y --> (/,open,$x,_)>>.  

'Lock-1 is a lock.
<{lock1} --> lock>. 

20

'Lock-1 can be opened by every key.
''outputMustContain('<<$x --> key> ==> <{lock1} --> (/,open,$x,_)>>. %1.00;0.81%')
