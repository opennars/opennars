package nars.rdfowl;

/**
 * Created by me on 6/9/15.
 */
public class NQuadsGraph {
	/*
	 * 
	 * 
	 * SpanGraph<?> st = new SpanGraph("spacetime", p); System.out.println(st);
	 * 
	 * for (Edge e : st.edgeSet()) { Vertex subj = e.getVertex(Direction.OUT);
	 * Vertex pred = e.getVertex(Direction.IN); System.out.println(subj +
	 * " --> " + pred); Term term = Inheritance.make(
	 * Atom.the(subj.getId().toString()), Atom.the(pred.getId().toString()) );
	 * System.out.println("  " + term); //sharedBag.put(term, new
	 * DefaultConcept(term)); nar.input(term + "."); nar.frame(1);
	 * 
	 * }
	 */
}
